# Commander app icon generation brief

The user restored the smooth Commander icon with a transparent background
on September 10, 2026. `commander-icon-transparent.png` is the active source.
See [the final imagegen prompt](commander-icon-transparent.prompt.md).

Background removal used the built-in image_gen tool and the original smooth
cap icon (`commander-icon-source.png`). The alpha channel is verified; launcher
exports preserve transparency and use smooth Lanczos scaling.
